{
  "name": "gpx-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@turf/turf": "^7.3.4",
    "leaflet": "^1.9.4",
    "leaflet-gpx": "^2.2.0",
    "preact": "^10.29.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.4",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.1"
  }
}
